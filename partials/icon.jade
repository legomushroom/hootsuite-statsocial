#{el.tagName || 'div'}(href="#{el.link || ''}", class="hst-icon #{ el.class || '' }", id="#{ el.idAttribute || el.id || '' }", title="#{ el.title || '' }")
  svg(viewBox="#{el.viewBox || '0 0 32 32'}")
    use(xlink:href="##{el.path || '' }-path")